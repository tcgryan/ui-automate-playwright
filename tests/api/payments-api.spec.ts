import { test } from '@playwright/test';

test.describe('transactions tests', () => {
  test('creates a transaction for a sale', async ({ request }) => {
    
  });

  test('creates a transaction for authorization only', async ({ request }) => {
    
  });

  test('captures already authorized transaction', async ({ request }) => {
    
  });

});